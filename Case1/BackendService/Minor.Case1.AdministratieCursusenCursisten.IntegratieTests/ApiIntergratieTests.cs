using Microsoft.AspNetCore.Hosting;
using Microsoft.AspNetCore.TestHost;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using Minor.Case1.AdministratieCursusenCursistenApi;
using System;
using System.Collections.Generic;
using System.IO;
using System.Linq;
using System.Threading.Tasks;

namespace Minor.Case1.AdministratieCursusenCursisten.IntegratieTests
{
    [TestClass]
    public class ApiIntergratieTests
    {
        //[TestMethod]
        //public async Task Get()
        //{
        //    // Arrange
        //    var _server = new TestServer(new WebHostBuilder()
        //        .UseStartup<Startup>());
        //    var _client = _server.CreateClient();

        //    // Act
        //    var response = await _client.GetAsync("http://localhost:32326/api/CursusInstantie");
        //    response.EnsureSuccessStatusCode();

        //    var responseString = await response.Content.ReadAsStringAsync();

        //    // Assert
        //    Assert.AreEqual("..some json..", responseString);
        //}
    }
}
